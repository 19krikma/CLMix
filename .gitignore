__pycache__/
*.pyc
.venv/
.venv-build/

# Windows packaging build output
/build/
/dist/
packaging/windows/dist_installer/
packaging/linux/dist_deb/

# Android
android/.gradle/
android/.idea/
android/local.properties
android/**/build/
android/**/*.iml
android/keystore.properties
android/*.keystore
android/*.jks

# iOS - CLMix.xcodeproj is generated from ios/project.yml via `xcodegen
# generate` (run from ios/), so it's reproducible rather than hand-typed -
# regenerate it instead of committing it.
ios/CLMix.xcodeproj/
ios/build/
